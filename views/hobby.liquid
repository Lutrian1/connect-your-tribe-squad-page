{% render 'partials/head.liquid', title: 'Hobby' %}

<h1>Hobby</h1>

<h2>Iedereen met de hobby: {{hobby.}}</h2>

<ul>
  {% for person in persons %}
    <li><a href="/student/{{ person.id }}">{{ person.name }}</a></li>
  {% endfor %}
</ul>
